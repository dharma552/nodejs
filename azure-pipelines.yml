trigger:
- main

pool:
  name: Default   # 👈 EXACT agent pool name

steps:
- checkout: self

- bash: |
    node -v
    npm -v
    npm install
    node index.js
  displayName: "Run Node.js app"





